#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>


float mixerToBackAngle(float rollAngle, float pitchAngle, float heightAngle)
{
	float BackAngle;
	
	
}

float mixerToRightAngle(float rollAngle, float pitchAngle, float heightAngle)
{
	float 
	if(rollAngle != null)
	
}

float mixerToLeftAngle(float rollAngle, float pitchAngle, float heightAngle)
 {
 	float 
 	if(rollAngle != null)
 	
 }
